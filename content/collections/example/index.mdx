# Example collection

This is a stub article for Collections View. Replace it with your real essay.

## A section

Normal Markdown works here: headings, **bold**, _italic_, [links](https://example.com),
lists, blockquotes, and images.

Because this is **MDX**, you can also embed React components later (e.g. a clickable
map link), once those components are built and registered in `mdx-components.tsx`.
